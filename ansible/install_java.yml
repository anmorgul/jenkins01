---

- name: Playbook for java installation
  gather_facts: true
  hosts: jenkinsnode
  remote_user: vagrant
  serial: 3

  tasks:
    - name: Install java
      become: true
      ansible.builtin.apt:
        name:
          - openjdk-11-jdk
        state: present
        update_cache: yes